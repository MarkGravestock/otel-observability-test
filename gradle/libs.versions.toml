[versions]
spring-boot = "4.0.4"
otel        = "2.26.1-alpha"

[libraries]
# BOMs (applied via platform() in build.gradle.kts)
otel-bom = { module = "io.opentelemetry.instrumentation:opentelemetry-instrumentation-bom-alpha", version.ref = "otel" }

# OTel (version managed by OTel BOM)
otel-logback-appender = { module = "io.opentelemetry.instrumentation:opentelemetry-logback-appender-1.0" }

# Spring Boot starters (version managed by Spring Boot BOM)
spring-boot-starter-web      = { module = "org.springframework.boot:spring-boot-starter-web" }
spring-boot-starter-actuator = { module = "org.springframework.boot:spring-boot-starter-actuator" }
spring-boot-starter-data-jpa = { module = "org.springframework.boot:spring-boot-starter-data-jpa" }
spring-boot-starter-test     = { module = "org.springframework.boot:spring-boot-starter-test" }
spring-boot-docker-compose   = { module = "org.springframework.boot:spring-boot-docker-compose" }
spring-boot-devtools         = { module = "org.springframework.boot:spring-boot-devtools" }

# Other (version managed by Spring Boot BOM)
mysql-connector-j = { module = "com.mysql:mysql-connector-j" }
lombok            = { module = "org.projectlombok:lombok" }

[plugins]
spring-boot = { id = "org.springframework.boot", version.ref = "spring-boot" }